import Navbar from "../../components/Navbar.jsx";

// TODO: Product form — fields: name, type (Goods/Service/Combo), category, salesPrice, cost
// Reference implementation to copy from: pages/Contacts/ContactList.jsx (list) and ContactForm.jsx (form)
export default function ProductForm() {
  return (
    <div>
      <Navbar />
      <div className="p-8">
        <h1 className="text-xl font-semibold mb-4">ProductForm</h1>
        <p className="text-gray-500 text-sm">TODO: Product form — fields: name, type (Goods/Service/Combo), category, salesPrice, cost</p>
      </div>
    </div>
  );
}
