import Navbar from "../../components/Navbar.jsx";

// TODO: Products list — copy pattern from Contacts/ContactList.jsx, using productsApi
// Reference implementation to copy from: pages/Contacts/ContactList.jsx (list) and ContactForm.jsx (form)
export default function ProductList() {
  return (
    <div>
      <Navbar />
      <div className="p-8">
        <h1 className="text-xl font-semibold mb-4">ProductList</h1>
        <p className="text-gray-500 text-sm">TODO: Products list — copy pattern from Contacts/ContactList.jsx, using productsApi</p>
      </div>
    </div>
  );
}
