import Navbar from "../../components/Navbar.jsx";

// TODO: List + create Budgets (name, period, analytic account, responsible person, committed amount)
// Reference implementation to copy from: pages/Contacts/ContactList.jsx (list) and ContactForm.jsx (form)
export default function BudgetList() {
  return (
    <div>
      <Navbar />
      <div className="p-8">
        <h1 className="text-xl font-semibold mb-4">BudgetList</h1>
        <p className="text-gray-500 text-sm">TODO: List + create Budgets (name, period, analytic account, responsible person, committed amount)</p>
      </div>
    </div>
  );
}
